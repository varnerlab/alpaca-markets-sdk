# Market Data

Historical and latest bars, quotes, and trades. All functions accept either a
single symbol or a vector of symbols, and default to the free `"iex"` feed.

```@docs
get_bars
get_quotes
get_trades
get_latest_bar
get_latest_quote
get_latest_trade
get_snapshot
```
